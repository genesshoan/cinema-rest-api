/**
 * dev.genesshoan.cinema_rest_api
 *
 * Core application package for the Cinema REST API.
 *
 * Sub-packages implement the application's layered architecture.
 * This package contains the main application entry point and global
 * configuration.
 *
 * High-level responsibilities:
 * - Defining global beans and application-wide configuration for the service.
 * - Bootstrapping the Spring Boot application (controllers, services,
 * repositories, DTOs, entities, mappers and error handling).
 */
package dev.genesshoan.cinema_rest_api;
