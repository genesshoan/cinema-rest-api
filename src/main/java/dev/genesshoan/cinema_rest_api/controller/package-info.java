/**
 * dev.genesshoan.cinema_rest_api.controller
 *
 * REST controllers for the Cinema REST API.
 *
 * Responsibilities:
 * - Expose HTTP endpoints for clients to interact with application resources (movies, rooms, etc.).
 * - Validate and parse incoming HTTP requests.
 * - Delegate business logic to service layer components.
 * - Map service-layer results to response DTOs and appropriate HTTP status codes.
 * - Handle request-level concerns such as input validation and pagination.
 */
package dev.genesshoan.cinema_rest_api.controller;

