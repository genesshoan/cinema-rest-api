/**
 * dev.genesshoan.cinema_rest_api.entity
 *
 * Domain entity definitions that map to persistent storage (e.g., JPA entities).
 *
 * Responsibilities:
 * - Represent the core business objects (such as Movie and Room).
 * - Contain mapping annotations for ORM (e.g., JPA/Hibernate) and any persistence-related configuration.
 * - Keep persistence concerns separated from API DTOs and service logic.
 */
package dev.genesshoan.cinema_rest_api.entity;

